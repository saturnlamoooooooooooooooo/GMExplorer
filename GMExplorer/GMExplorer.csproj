<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net9.0</TargetFramework>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <AvaloniaUseCompiledBindingsByDefault>true</AvaloniaUseCompiledBindingsByDefault>
    <AssemblyName>GMExplorer</AssemblyName>
    <RootNamespace>GMExplorer</RootNamespace>
    <InvariantGlobalization>true</InvariantGlobalization>
    <PlatformTarget>x64</PlatformTarget>
    <ApplicationIcon>Assets\logo.ico</ApplicationIcon>
  </PropertyGroup>

  <ItemGroup>
    <AvaloniaResource Include="Assets\**" />
  </ItemGroup>

  <ItemGroup>
    <None Remove="C:\Users\monoc\.nuget\packages\naudio.vorbis\1.5.0\contentFiles\any\netstandard2.0\README.md" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Avalonia" Version="12.0.5" />
    <PackageReference Include="Avalonia.Desktop" Version="12.0.5" />
    <PackageReference Include="Avalonia.Themes.Fluent" Version="12.0.5" />
    <PackageReference Include="Avalonia.Fonts.Inter" Version="12.0.5" />
    <PackageReference Include="AvaloniaUI.DiagnosticsSupport" Version="2.2.3">
      <IncludeAssets Condition="'$(Configuration)' != 'Debug'">None</IncludeAssets>
      <PrivateAssets Condition="'$(Configuration)' != 'Debug'">All</PrivateAssets>
    </PackageReference>
    <PackageReference Include="SharpZipLib" Version="1.4.2" />
    <PackageReference Include="NAudio" Version="2.2.1" />
    <PackageReference Include="NAudio.Vorbis" Version="1.5.0" />
  </ItemGroup>
  <!-- Published builds carry the analyser inside the executable, so a release is one file. -->
  <ItemGroup Condition="Exists('..\native\bin\x64\Release\gmnative.dll')">
    <EmbeddedResource Include="..\native\bin\x64\Release\gmnative.dll" LogicalName="gmnative.dll" />
  </ItemGroup>

  <ItemGroup>
    <None Include="..\native\bin\x64\Release\gmnative.dll" Condition="Exists('..\native\bin\x64\Release\gmnative.dll')" Link="gmnative.dll" CopyToOutputDirectory="PreserveNewest" />
    <None Include="..\native\bin\x64\Debug\gmnative.dll" Condition="!Exists('..\native\bin\x64\Release\gmnative.dll') And Exists('..\native\bin\x64\Debug\gmnative.dll')" Link="gmnative.dll" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

  <!-- A single-file publish should really be a single file: drop the native .pdb files and
       anything else the runtime packs bring along. -->
  <Target Name="TrimPublishFolder" AfterTargets="Publish" Condition="'$(PublishSingleFile)' == 'true'">
    <ItemGroup>
      <PublishLeftovers Include="$(PublishDir)**\*" Exclude="$(PublishDir)$(AssemblyName)$(_NativeExecutableExtension);$(PublishDir)$(AssemblyName).exe" />
    </ItemGroup>
    <Delete Files="@(PublishLeftovers)" />
    <Message Importance="high" Text="Published $(PublishDir)$(AssemblyName).exe" />
  </Target>

</Project>
